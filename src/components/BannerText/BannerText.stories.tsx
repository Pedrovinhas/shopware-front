import type { Meta, StoryObj } from '@storybook/react'
import { BannerText, BannerTextProps } from '.'

export default {
  title: 'Elements/BannerText',
  component: BannerText,
  args: {
  },
  argTypes: {
    
  },
 
} as Meta<BannerTextProps>

export const Custom: StoryObj<BannerTextProps> = {}
